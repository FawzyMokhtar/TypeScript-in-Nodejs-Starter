/**
 * Represents a products category update request data-model.
 */
export interface UpdateCategoryInput {
  /**
   * Gets or sets the id of the category.
   */
  id: number;

  /**
   * Gets or sets the name of category.
   */
  name: string;
}
